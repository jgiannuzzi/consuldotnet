name: CI

on:
  push:

jobs:
  build:
    if: github.event_name == 'push' || github.event.pull_request.head.repo.id != github.event.pull_request.base.repo.id
    strategy:
      matrix: ${{ fromJson('{"os":["ubuntu-18.04","windows-2019","macos-10.15"],"component":["Consul","Consul.AspNetCore"],"consul":["1.6.10","1.7.10","1.8.6","1.9.0"],"dotnet":["2.1.811","3.1.404","5.0.101"],"exclude":[{"component":"Consul.AspNetCore","consul":"1.7.10"},{"component":"Consul.AspNetCore","consul":"1.8.6"},{"component":"Consul.AspNetCore","consul":"1.9.0"},{"component":"Consul.AspNetCore","dotnet":"2.1.811"}],"include":[{"os":"windows-2019","component":"Consul","dotnet":"4.7","consul":"1.6.10"},{"os":"windows-2019","component":"Consul","dotnet":"4.7","consul":"1.7.10"},{"os":"windows-2019","component":"Consul","dotnet":"4.7","consul":"1.8.6"},{"os":"windows-2019","component":"Consul","dotnet":"4.7","consul":"1.9.0"}]}') }}
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - name: Debug
        shell: bash
        run: |
          echo "os: ${{ matrix.os }}"
          echo "component: ${{ matrix.component }}"
          echo "consul: ${{ matrix.consul }}"
          echo "dotnet: ${{ matrix.dotnet }}"
